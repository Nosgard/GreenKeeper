<Window x:Class="GreenKeeper.Views.Notes.NotesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GreenKeeper.Views.Notes"
        mc:Ignorable="d"
        Title="NotesView" Height="450" Width="500"
        Background="#1A1C22"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Section for the plant name -->
        <TextBlock Grid.Row="0"
               Text="{Binding PlantName}"
               Foreground="#F0F0EE"
               FontSize="20"
               FontWeight="SemiBold"
               Margin="0, 0, 0, 12" />

        <!-- Notes of the plant -->
        <TextBox Grid.Row="1"
               Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
               Background="#2C2C2E"
               Foreground="#F0F0EE"
               CaretBrush="#F0F0EE"
               BorderThickness="1"
               BorderBrush="#3A3D47"
               Padding="10"
               FontSize="14"
               AcceptsReturn="True"
               TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto" />

        <!-- Button area -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0, 12, 0, 0">
            
            <Button Style="{StaticResource MediumDarkButton}"
                    Content="Save"
                    Padding="10" />

            <Button Style="{StaticResource MediumDarkButton}"
                    Content="Cancel"
                    Margin="10, 0, 0, 0"
                    Padding="10"
                    IsCancel="True" />
        </StackPanel>

    </Grid>
</Window>
